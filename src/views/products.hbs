<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            justify-content: space-around;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .form-card {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 2%;
            padding: 2%;
            width: 20%;
            display: flex;
            flex-direction: column;
            text-align: left;
        }

        .header-card {
            display: flex;
            flex-direction: row;
            justify-content: space-around;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 2%;
            padding: 2%;
            padding-inline: 15%;
            gap: 5px;
            width: auto;
        }

        .Productos {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
        }

        h1 {
            text-align: center;
            color: #333;
        }

    
        label,
        input,
        button {
            margin-bottom: 10px;
            display: block;
        }

        button {
            padding: 10px;
            background-color: #4CAF50;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }


        .btn-verMas{
            background-color: orange;
            padding: 3%;
            color: black;
            border-radius: 4px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-decoration: none;
            margin-inline: 30%;
            margin-block: 10px;
            text-align: center;
        }

        .carrito{
            background-color: orange;
            padding: 3%;
            color: black;
            border-radius: 4px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            font-weight: bolder;
            width: 50%;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>CODERTIENDA</h1>
    </div>

    <div class="header-card">
        <form method="get" action="/api/products">
            <label for="limit">CANTIDAD PRODUCTOS POR PÁGINA</label>
            <input type="number" name="limit">
            <button type="submit">ENVIAR</button>
        </form>

        <form method="get" action="/api/products">
            <label for="page">ELEGIR PÁGINA</label>
            <input type="number" name="page">
            <button type="submit">ENVIAR</button>
        </form>

        <form method="get" action="/api/products">
            <label for="category">FILTRO CATEGORÍA</label>
            <input type="text" name="category">
            <button type="submit">ENVIAR</button>
        </form>

        <form method="get" action="/api/products">
            <label>ORDENAR POR PRECIO</label>
            <button type="submit" name="sortDirection" value="asc"> DE MENOR A MAYOR</button>
            <button type="submit" name="sortDirection" value="des"> DE MAYOR A MENOR</button>
        </form>
    </div>

    <div class="carrito">
        ARTICULOS EN CARRITO DE COMPRAS: {{cart.length}}
    </div>


    <div class="Productos">
        {{#each productos}}
        <div class="form-card">
            <p>Nombre:{{this.nombre}} </p>
            <p>Descripción: {{this.descripcion}}</p>
            <p>Categoría: {{this.category}}</p>
            <p>Precio: {{this.precio}}</p>
            <p>Stock: {{this.stock}}</p>

            <a href="/api/products/{{this._id}}" class="btn-verMas"> VER MÁS</a>
        </div>
        {{/each}}
    </div>
</body>
</html>